<%= error_messages_for :lexeme %>

<%= @action = "Create"
		render(:partial => "form", :object => { :label => t('.create'), :action => 'create', :method => :post, :for_url => "/#{I18n.locale}/lexemes" }) 
		%>

<%= link_to t('.back'), lexemes_path %>
