<%= 
    unless (f.object.try(:_destroy)) 
        if form.present? && form == :interlinear
            render partial: 'parses/shortform', locals: {f: f}
        else
            render partial: 'parses/lexemeform', locals: {f: f}
        end
    end
%>