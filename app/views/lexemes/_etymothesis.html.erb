<% unless (f.object.try(:_destroy)) %>
	<%= list_children_with_option_to_add :etymology, f, remote: true, limit_one: true %>
<fieldset class="etymothesis">
    <legend><%= label_with_remove_option_for "Etymology", f %></legend>
    
    <%= list_children_with_option_to_add :source, f, remote: true, limit_one: true %>
</fieldset>
<% end %>