<%= error_messages_for :locus %>

<%=	render(:partial => "form", :object => { :label => t('.update'), :action => 'update', :method => :put, :for_url => "/#{I18n.locale}/loci/#{@locus.id}" }) %>

<%= link_to t('.show'), @locus %> |
<%= link_to t('.back'), loci_path %>
