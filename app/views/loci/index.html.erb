<% if @loci %>
<table width="100%">
<% @loci.each_with_index do |locus, i| %>
	<tr>
		<td colspan="3">
			<%= render(:partial => "shared/locus", :object => locus, :locals => { :locus_counter => locus.id - 1 })%>
		</td>
	</tr>
<% end %>
</table>

<br />

<%= link_to t('.new_locus'), new_locus_path %>

<%= will_paginate @loci %>
<% else %>
<p><i><%= t('.no_loci') %></i></p>
<% end %>
