<h1>Editing language</h1>

<%= error_messages_for :language %>

<%=	render(:partial => "form", :object => { :label => 'Update', :action => 'update', :method => :put, :for_url => "/languages/#{@language.id}" }) %>

<%= link_to 'Show', @language %> |
<%= link_to 'Back', languages_path %>
