<%= error_messages_for :language %>

<%= # @action = "Create"
		render(:partial => "form", :object => { :label => t('languages.new.create_button', default: 'Create'), :action => 'create', :method => :post, :for_url => "/#{I18n.locale}/languages" }) 
		%>

<%= link_to t("languages.new.back", :default => 'Back'), languages_path %>
