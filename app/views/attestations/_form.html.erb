<%= 
fields_for @path, @attestation, :child_index => Time.now.to_i.to_s do |f|
    width = (f.object.attested_form.try(:size) || 25) * 1.2
    render partial: 'shortform', locals: { f: f, width: width }
end 
%>