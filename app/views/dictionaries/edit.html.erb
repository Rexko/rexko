<%= error_messages_for :dictionary %>

<%=	render(:partial => "form", :object => { :label => t('.update'), :action => 'update', :method => :put, :for_url => "/#{I18n.locale}/dictionaries/#{@dictionary.id}" }) %>

<%= link_to t('.show'), @dictionary %> |
<%= link_to t('.back'), dictionaries_path %>
