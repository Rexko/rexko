<%= 
    fields_for @path, @etymology, :child_index => Time.now.to_i.to_s do |f|  
        width ||= sanitize(params[:width])
        render partial: 'shortform', locals: {f: f, width: width}
    end 
%>