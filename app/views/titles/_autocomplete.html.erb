<ul>
    <% @titles.each do |t| %>
        <li id="<%= t.id %>-<%= @ref %>"><%= t.name %></li>
    <% end %>
        <li id="new-<%= @ref %>" class="highlight"><i><%= t('.new_title') %></i></li>
</ul>