<div class="gloss">
    <div class="type-text">
        <%= f.label :gloss, label_with_remove_option_for(t('.gloss'), f) %>
        <%= translatable_tag f, :text_field, :gloss, :target %>
    </div>

    <%= list_children_with_option_to_add :parse, f, :remote => true, :locals => { dictionaries: @dictionaries } %>
</div>